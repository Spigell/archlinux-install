lvm:
  vg: vg_main
  lv: slashroot
grub:
  install: true
  type: efi
  target: /dev/sda
  partition: /dev/sda2
packages:
  - openssh
  - sudo
  - NetworkManager
enable-services:
  - ssh
  - NetworkManager
  
